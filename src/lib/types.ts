export type VehicleDetails = {
  userName: string;
  mobile: string;
  address: string;
  vehicleModel: string;
  licensePlate: string;
};

export type ServiceItem = {
  id: string;
  description: string;
  partsCost: number;
  laborCost: number;
};
